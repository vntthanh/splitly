{
  "name": "splitly-ocr-bill-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "node scripts/check.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  }
}
