MONGODB_URI=
DATABASE_NAME=
# APP_HOST=localhost
# APP_PORT=8017
MONGODB_USER=
MONGODB_PASSWORD=

# Email Configuration (using Nodemailer)
ADMIN_EMAIL_ADDRESS='noreply@splitly.com'
ADMIN_EMAIL_NAME='Splitly'

# SMTP Configuration for Nodemailer
# Gmail: smtp.gmail.com (port 465 or 587)
# Outlook: smtp.office365.com (port 587)
# SendGrid: smtp.sendgrid.net (port 587)
# Mailgun: smtp.mailgun.org (port 587)
SMTP_HOST='smtp.gmail.com'
SMTP_PORT=465
SMTP_USER='your-email@gmail.com'
SMTP_PASSWORD='your-app-password'  # For Gmail, use App Password (not regular password)

# Legacy Brevo API (not used anymore, but kept for reference)
BREVO_API_KEY=

ACCESS_JWT_SECRET_KEY='your_jwt_secret_key_here'
ACCESS_JWT_EXPIRES_IN='1h'


WEBSITE_DOMAIN_DEVELOPMENT='http://localhost:5173'
# Comma-separated frontend origins allowed in production, without a trailing slash.
WEBSITE_DOMAIN_PRODUCTION='https://your-frontend-domain.com'